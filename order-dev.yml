spring:
  application:
    name: order
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql://192.168.1.23/sell?characterEncoding=utf-8&useSSl=false
  jpa:
      show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8081

PRODUCT:
  ribbon:
    NFLoadBalanceRulerClassName: com.netflix.loadbalancer.RandomRule
dev:
  dev
